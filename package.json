{
  "name": "codecarto-monorepo",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.30.3",
  "scripts": {
    "build": "turbo run build",
    "build:site": "node site/build.mjs",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "catalog:"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
